# Project instructions

- Write Git commit messages in Vietnamese without diacritics, as requested by the user.
- Follow `NCKH_Knowledge_Tracing_AI_Agent_Plan_v3_Consolidated.md` and the staged configuration locks.
- Reuse the saved student split. Never tune models, prompts or policies using TEST.
- Keep raw data, student identifiers, model checkpoints and per-row predictions out of Git.
- For parallel work, assign disjoint file ownership and run the integrated test suite before committing.
